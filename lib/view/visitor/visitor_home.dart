import 'package:flutter/material.dart';

class VisitorHome extends StatelessWidget{
  const VisitorHome({super.key});

  @override
  Widget build(BuildContext context) {

    return  Scaffold(
      body: Text('visitor_home')
    );
  }
}