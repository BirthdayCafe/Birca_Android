import 'package:flutter/material.dart';

class VisitorCafeTour extends StatelessWidget{
  const VisitorCafeTour({super.key});

  @override
  Widget build(BuildContext context) {

    return const Scaffold(
        body: Text('visitor_cafe_tour')
    );
  }
}