import 'package:flutter/material.dart';

class VisitorMyPage extends StatelessWidget{
  const VisitorMyPage({super.key});

  @override
  Widget build(BuildContext context) {

    return const Scaffold(
        body: Text('visitor_mypage')
    );
  }
}