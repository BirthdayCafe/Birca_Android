import 'package:flutter/material.dart';

class VisitorFavorite extends StatelessWidget{
  const VisitorFavorite({super.key});

  @override
  Widget build(BuildContext context) {

    return const Scaffold(
        body: Text('visitor_heart')
    );
  }
}