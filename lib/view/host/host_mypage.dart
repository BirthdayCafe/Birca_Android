import 'package:flutter/material.dart';

class HostMyPage extends StatelessWidget{
  const HostMyPage({super.key});

  @override
  Widget build(BuildContext context) {

    return const Scaffold(
        body: Text('HostMyPage')
    );
  }
}